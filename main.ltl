// ltl-spline/main.ltl
//
// Cubic-spline interpolation (natural + clamped).
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-spline v0.1.0")
    io::println("Cubic-spline interpolation (natural + clamped).")
    0
}
