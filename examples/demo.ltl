// examples/demo.ltl — runnable demo for ltl_spline

import std::io
use ltl_spline as lib

fn main() {
    io::println("ltl_spline demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}